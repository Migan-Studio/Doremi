export const secRegex = /^[0-9]{1,2}(?:\s)?(?:초|sec|s)$/i
export const minRegex = /^[0-9]{1,2}(?:\s)?(?:분|min|m|minute|minutes)$/i
export const hourRegex = /^[0-9]{1,2}(?:\s)?(?:시간|hour|h|hours)$/i
export const dayRegex = /^[0-9]{1,2}(?:\s)?(?:일|day|d|days)$/i
export const weekRegex = /^[0-9]{1,2}(?:\s)?(?:주|week|w|weeks)$/i
export const dicimal = /^[0-9]/g
